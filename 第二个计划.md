# 第二个计划：从 DiffMS-Mini 复现转向论文实验复现与新 idea 探索

## 一、现在的真实情况

目前已经完成了一个 DiffMS-Mini 版本，Day8-Day13 的主要代码、日志、图表都已经生成。但是需要明确一点：Day10-Day13 的代码主要是在 AI 辅助下完成的，我对其中很多细节还没有完全掌握。因此，接下来的任务不能直接继续堆代码，而是要先把“已经跑出来的东西是什么意思”弄清楚，再根据老师的要求补论文里的正式实验图和表。

现在项目处于这个阶段：

1. mini pipeline 已经跑通：质谱数据可以经过谱编码器，接到图解码器，生成分子图。
2. 当前结果显示：生成分子的合法率较高，但是和真实分子的结构相似度很低。
3. 老师关心的不只是 mini 版本能不能跑，而是论文 Table 1 的正式评测指标和分子可视化结果有没有跑出来。
4. 下一步要从“简化版复现”过渡到“论文实验复现 + 指标理解 + 寻找改进点”。

## 二、老师昨天布置的新任务到底是什么意思

老师发的聊天记录里主要有两个任务。

### 任务 1：跑论文 Table 1 里的指标

论文 Table 1 比较了 DiffMS 和其他 baseline 方法，比如 Spec2Mol、MADGEN、MIST + Neuraldecipher、MIST + MSNovelist 等。表里有这些指标：

| 指标 | 含义 | 越大/越小越好 | 我需要理解到什么程度 |
|------|------|----------------|------------------------|
| Top-1 Accuracy | 模型排名第一的分子是否和真实分子完全一致 | 越大越好 | 知道它是严格命中率，不是相似就算对 |
| Top-10 Accuracy | 前10个候选分子里是否有真实分子 | 越大越好 | 知道它衡量多候选生成能力 |
| Tanimoto | 生成分子和真实分子的指纹相似度 | 越大越好 | 知道它衡量“像不像” |
| MCES | 分子图编辑距离 | 越小越好 | 知道它衡量生成分子要改多少才能变成真实分子 |

老师说“对比的一些方法的指标也需要自己跑一下”，意思不是让我重新发明这些方法，而是要先确认原仓库有没有提供评测脚本、预测结果或 baseline 输出，然后尝试在本地或服务器上复现这些指标。

### 任务 2：跑论文里的分子图可视化

老师发的另一个图是论文 Figure 4，内容是：左边是真实分子 Ground Truth，右边是模型生成的 Top-1、Top-2、Top-3、Top-4 候选分子，并标注每个候选的 Tanimoto 和 MCES。

这个图的目的不是单纯画漂亮图片，而是为了直观看模型到底生成了什么：

1. 如果 Top-1 和真实分子一样，说明模型成功重构。
2. 如果 Top-k 中有分子和真实分子很像，说明模型虽然没完全猜对，但方向接近。
3. 如果生成分子合法但完全不像，说明条件控制能力不足。

我目前还没有真正跑出这种“Ground Truth vs Top-k 生成分子”的可视化图，这个是接下来必须补的。

## 三、Day8-Day13 每天跑出来的东西代表什么

### Day8：图解码器预训练

输入：真实分子的 Morgan 指纹 + 加噪后的分子图。

输出：预测每两个原子之间是什么键。

目的：训练一个图解码器，让它学会从条件信息和当前噪声图中恢复分子结构。

我跑出来的结果：loss 下降到约 0.30，validity 约 92%。

这说明：图解码器初步学会生成合法分子，但这还不是完整的“质谱生成分子”，因为它的条件来自真实指纹，不是质谱。

### Day9：谱图编码器训练

输入：质谱峰向量，长度 1000。

输出：分子 Morgan 指纹，长度 2048。

目的：让模型从质谱里预测分子结构特征。

我跑出来的结果：测试集平均 Tanimoto 约 0.1752。

这说明：谱图编码器能学到一点谱图和分子结构之间的关系，但能力还比较弱。

### Day10：端到端连接

输入：质谱峰向量。

中间过程：谱图编码器先预测指纹，再把这个指纹作为条件传给图解码器。

输出：生成分子图。

目的：把“质谱 -> 指纹”和“指纹 -> 分子图”接成一个完整 pipeline。

我跑出来的结果：Validity 100%，Tanimoto 约 0.0015。

这说明：模型能生成合法分子，但生成的分子和真实分子几乎不像。

### Day11：端到端微调和 Top-k 采样

输入：同一个质谱。

输出：多个候选分子。

目的：模拟论文里的 Top-k 生成，看前几个候选里面有没有接近真实分子的。

我跑出来的结果：Top-1、Top-5、Top-10 Tanimoto 都为 0，Validity 100%。

这说明：当前 mini 版本虽然能生成合法分子，但多候选没有带来有效提升，说明采样多样性或条件约束可能有问题。

### Day12：消融实验

目的：比较不同预训练设置对结果的影响。

当前结果：

| 组别 | Validity | Tanimoto |
|------|---------:|---------:|
| 完整预训练 | 100.00% | 0.0015 |
| 无预训练 | 100.00% | 0.0015 |
| 仅解码器预训练 | 100.00% | 0.0037 |
| 仅编码器预训练 | 100.00% | 0.0015 |

这说明：预训练对合法性没有明显影响，因为四组都是 100%；仅解码器预训练略微提高 Tanimoto，但整体仍然很低。问题核心仍然是生成结构不对。

### Day13：可视化和失败分析

目的：把 Day8-Day12 的结果整理成图、表和结论，方便汇报。

当前作用：用于说明 mini 版本已经跑通，但性能和论文正式结果差距很大，需要进入下一阶段复现和改进。

## 四、接下来要补的两个核心交付物

### 交付物 1：Table 1 指标复现表

目标：至少先跑通小样本的 Accuracy、Tanimoto、MCES、Top-k 计算流程。

最低完成标准：

1. 能读取测试集 labels.tsv。
2. 能找到每条谱图对应的真实 SMILES。
3. 能读取或生成候选分子 SMILES。
4. 能计算 Top-1 / Top-10 Accuracy。
5. 能计算 Top-1 / Top-10 Tanimoto。
6. 能计算或调用 MCES。
7. 能输出一个和论文 Table 1 格式类似的结果表。

理想完成标准：

1. 能跑 DiffMS 原仓库提供的 evaluation 脚本。
2. 能复现论文里 DiffMS 在某个数据集上的部分指标。
3. 能对至少一个 baseline 方法跑出可比较结果。

### 交付物 2：Ground Truth vs Top-k 分子可视化图

目标：仿照论文 Figure 4，把真实分子和生成候选分子画出来。

最低完成标准：

1. 选 3-5 条测试样本。
2. 每条样本画出 Ground Truth 分子。
3. 每条样本画出 Top-1 到 Top-4 候选分子。
4. 每个候选下面标注 Tanimoto。
5. 如果 MCES 暂时跑不通，可以先空着或标注“待计算”。

理想完成标准：

1. 每个候选同时标注 Tanimoto 和 MCES。
2. 能展示成功案例和失败案例。
3. 能解释为什么有些候选合法但不接近真实结构。

## 五、接下来每一步具体怎么做

### Step 1：重新理解 Day10-Day13 代码

时间：0.5-1 天。

目标：不是重新写所有代码，而是把每个文件的输入、输出、核心函数搞懂。

需要弄清楚：

| 文件 | 我需要知道什么 |
|------|----------------|
| 10_diffms_mini.py | 谱编码器和图解码器是怎么接起来的 |
| 11_finetune.py | 端到端训练时 loss 是怎么算的 |
| 11_sample.py | Top-k 候选分子是怎么生成的 |
| 12_ablation.py | 四组消融实验差异在哪里 |
| 13_visualization.py | 最终图表是怎么汇总的 |

完成标准：我能用自己的话解释这五个文件分别在做什么。

### Step 2：检查原仓库有没有 Table 1 评测代码

时间：0.5-1 天。

目标：找到 evaluation、metrics、baseline 相关代码。

重点搜索关键词：

- evaluate
- eval
- metrics
- topk
- tanimoto
- mces
- accuracy
- baseline
- retrieval
- MassSpecGym
- NPLIB1

完成标准：整理一个表，写清楚每个指标对应哪个脚本或函数。

### Step 3：观察正式数据集结构

时间：0.5 天。

目标：弄懂老师截图里的数据集。

重点看这些文件夹和文件：

| 文件/文件夹 | 作用 |
|-------------|------|
| labels.tsv | 每条谱图对应的真实分子标签 |
| spec_files | 原始质谱文件 |
| subformulae | 子式分解或预处理后的谱图信息 |
| splits | train/val/test 划分 |
| retrieval_hdf | 可能用于检索或候选生成的数据 |
| magma_outputs | 可能是 MAGMa 注释结果 |

完成标准：能说清楚一条样本从 specname 到真实 smiles，再到谱图文件，是怎么对应起来的。

### Step 4：先跑小样本指标计算

时间：1-2 天。

目标：不要一开始跑完整数据集，先拿 10-50 条样本测试指标流程。

要输出：

1. 小样本 Top-1 Accuracy。
2. 小样本 Top-10 Accuracy。
3. 小样本平均 Tanimoto。
4. 小样本平均 MCES，如果 MCES 工具能跑通。
5. 一个 CSV 结果表。

完成标准：即使结果很低，也要保证指标计算流程是正确的。

### Step 5：跑分子可视化图

时间：1 天。

目标：仿照论文 Figure 4，画出真实分子和 Top-k 候选分子。

要输出：

1. images/ground_truth_topk_examples.png。
2. 每个样本的真实 SMILES。
3. 每个候选的生成 SMILES。
4. 每个候选的 Tanimoto 和 MCES。

完成标准：这张图可以直接发给老师看。

### Step 6：决定是否需要服务器

时间：0.5 天。

判断标准：

| 情况 | 做法 |
|------|------|
| 只是跑评测脚本 | 本地优先 |
| 需要训练完整 DiffMS | 建议服务器 |
| 需要跑全部 baseline | 大概率需要服务器 |
| 只做小样本验证 | 本地可以 |

需要准备：

1. 服务器环境要求。
2. 数据集路径。
3. 运行命令。
4. 预计耗时。
5. 需要保存的输出文件。

### Step 7：寻找可以写成论文的小 idea

时间：2-4 周持续推进。

当前最适合的 idea 方向：

1. 在谱图条件之外加入 formula / ionization / precursor mass 条件。
2. 改进 Top-k 采样，让候选分子更多样。
3. 做失败案例分析，找出模型在哪类分子上容易失败。
4. 用轻量模型复现 DiffMS，并分析小模型和大模型差距。
5. 比较不同谱图向量化方式对 Tanimoto 的影响。

最现实的投稿方向不是“完整超越 DiffMS”，而是做一个小而明确的改进：

> 基于分子式和离子类型增强条件的质谱分子生成方法研究。

## 六、短期时间安排

### 第 1 天：任务拆解和代码理解

目标：把 Day10-Day13 的代码和老师新任务弄清楚。

交付物：

1. Day10-Day13 代码理解笔记。
2. Table 1 指标解释表。
3. 数据集结构说明。

### 第 2 天：找原仓库评测代码

目标：确认 Table 1 的指标怎么跑。

交付物：

1. evaluation 脚本清单。
2. 指标函数位置表。
3. 能否本地跑的判断。

### 第 3 天：小样本跑指标

目标：拿 10-50 条样本跑通 Top-k / Tanimoto / Accuracy。

交付物：

1. 小样本指标 CSV。
2. 小样本结果 md 日志。
3. 报错和解决记录。

### 第 4 天：补 MCES

目标：搞清楚 MCES 怎么安装、怎么调用、怎么计算。

交付物：

1. MCES 小样本结果。
2. 如果 MCES 暂时跑不通，要写明原因和替代方案。

### 第 5 天：画 Ground Truth vs Top-k 图

目标：生成论文 Figure 4 风格的分子图。

交付物：

1. 分子可视化 PNG。
2. 每个分子的 Tanimoto / MCES 标注。
3. 成功案例和失败案例说明。

### 第 6-7 天：整理阶段汇报

目标：把结果发给老师，并根据反馈决定是否上服务器。

交付物：

1. 阶段汇报 md。
2. 指标表。
3. 分子图。
4. 下一步服务器计划。

## 七、我需要达到的水平

### 最低水平

1. 能解释 Day8-Day13 每天做了什么。
2. 能说明当前 mini 版本为什么 Tanimoto 很低。
3. 能跑出 Table 1 相关指标的小样本版本。
4. 能画出 Ground Truth vs Top-k 分子图。
5. 能向老师说明下一步是否需要服务器。

### 合格科研训练水平

1. 能读懂原仓库 evaluation 代码。
2. 能复现论文中至少一部分正式评测流程。
3. 能比较 mini 版本和原论文结果的差距。
4. 能提出一个明确改进点。
5. 能做小规模对照实验验证改进是否有效。

### 投稿准备水平

1. 有完整实验对比表。
2. 有自己的改进方法。
3. 有消融实验。
4. 有可视化分析。
5. 有失败案例分析。
6. 有初步论文结构和实验结论。

## 八、当前最重要的判断

现在不是继续盲目训练，也不是急着说要投稿。当前最重要的是三件事：

1. 把已经做过的 Day10-Day13 真正理解清楚。
2. 把老师点名的两个图和表补出来。
3. 回到原论文 Table 1，跑通正式指标计算流程。

只有做到这三点，后面才知道新的 idea 应该从哪里来，也才知道需要不需要服务器。
